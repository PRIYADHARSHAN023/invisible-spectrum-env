[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "invisible-spectrum-env"
version = "0.1.0"
description = "Behavioral pattern recognition environment for subtle cognitive patterns"
readme = "README.md"
authors = [
    {name = "Priyan023"}
]
dependencies = [
    "pydantic>=2.7.4",
    "numpy",
    "fastapi",
    "uvicorn",
    "python-dotenv"
]
